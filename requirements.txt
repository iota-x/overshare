# Shared
discord.py>=2.3
requests>=2.31.0
python-dotenv>=1.0.0

# macOS
rumps>=0.4.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.0; sys_platform == "darwin"

# Windows
pywin32>=306; sys_platform == "win32"
psutil>=5.9; sys_platform == "win32"
pystray>=0.19; sys_platform == "win32"
pillow>=10.0; sys_platform == "win32"
# Windows ships no IANA timezone database — zoneinfo (used for "her timezone")
# needs this to resolve names like "America/New_York".
tzdata>=2024.1; sys_platform == "win32"
# Windows has no CLI equivalent to macOS's imagesnap for a webcam still, so
# !peek/!screen/!live use this for the camera (screen capture uses Pillow's
# ImageGrab instead — already a dependency above, no extra install for that).
opencv-python-headless>=4.8; sys_platform == "win32"
# Windows exposes no API for the active tab's URL. UI Automation can read it
# out of the address bar, which is what unlocks per-site cards, clickable links
# and YouTube thumbnails there. Optional — set READ_BROWSER_URL=false to skip.
uiautomation>=2.0.18; sys_platform == "win32"

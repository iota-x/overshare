## What changed

<!-- And why it mattered. -->

## How you checked it

<!-- Delete what doesn't apply. -->

- [ ] `python -m in_detail.selftest` still renders every card
- [ ] Opened the settings window (`python run_app.py --settings`) and the page I touched behaves
- [ ] Ran it for real on macOS
- [ ] Ran it for real on Windows

<!--
CI builds on both platforms, so you don't have to own both machines — but say
which one you actually tried it on. "Only tested on Windows" is useful and
completely fine.

If you added a setting, it needs a control in the settings window too — see
CONTRIBUTING.md. Nothing should require editing a file by hand.
-->
